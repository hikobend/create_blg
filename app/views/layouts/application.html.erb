<!DOCTYPE html>
<html>
  <head>
    <title>Myapp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <% if flash[:notice] %>
      <div class="alert alert-primary" role="alert"><strong><%= notice %></strong></div>
    <% end %>
    <% if flash[:alert] %>
      <div class="alert alert-danger" role="alert"><strong><%= alert %></strong></div>
    <% end %>
    <header>
      <div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
        <h2 class="my-0 mr-md-auto font-weight-normal">新米エンジニアの技術メモ</h2>
        </div>
        <div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
        <nav class="my-2 my-md-0 mr-md-3">
          <a class="p-2 text-dark" href="#">ホーム</a>
          <a class="p-2 text-dark" href="#">プロフィール</a>
          <a class="p-2 text-dark" href="#">タグ一覧・投稿数</a>
          <a class="p-2 text-dark" href="#">お問合せ</a>
        </nav>  
      </div>
    </header>
    <%= yield %>
    <footer>
      <% if user_signed_in? %>
        <%= link_to 'ログイン', new_user_session_path, class: "btn btn-outline-primary" %>
      <% else %>
        <%= link_to 'ログイン', new_user_session_path, class: "btn btn-outline-primary" %>
      <% end %>
    </footer>
  </body>
</html>
