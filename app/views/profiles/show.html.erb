<div class="container">
    <small class="text-muted">作成日 : <%= l @profile.created_at, format: :default %></small>
    <small class="text-muted">編集日 : <%= l @profile.updated_at, format: :default %></small>
  <hr />
  <div class="mb-3">
    <div class="field-form">
      <h3><%= markdown(@profile.profile).html_safe %></h3>
    </div>
  </div>
  <%= link_to '編集画面', edit_profile_path, class: 'btn btn-success' %>
</div>
